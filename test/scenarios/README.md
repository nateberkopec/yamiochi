# Scenario gates

Files in this directory are the human-owned holdout set described in `FACTORY.md`.

Day-one scenarios currently verify the factory contract itself so Fabro has a stable, deny-listed target while Yamiochi is still skeletal. Replace or expand them with real Rack/HTTP trajectories as the server becomes runnable.
