FROM ruby:3.3-bookworm

RUN apt-get update \
 && apt-get install -y --no-install-recommends \
      build-essential \
      ca-certificates \
      curl \
      git \
      pkg-config \
      ripgrep \
      shellcheck \
      python3 \
 && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
